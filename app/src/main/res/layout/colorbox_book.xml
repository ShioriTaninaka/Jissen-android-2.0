<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/body_colorBox"
    android:layout_width="190dp"
    android:layout_height="45dp">

    <ImageButton
        android:id="@+id/prevButton_mediaBox"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:layout_marginStart="16dp"
        android:layout_marginLeft="16dp"
        android:layout_marginTop="10dp"
        android:background="#00FFFFFF"
        android:scaleType="centerInside"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@drawable/yellowbox"
        tools:visibility="visible" />

    <ImageButton
        android:id="@+id/playButton_mediaBox"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:layout_marginStart="20dp"
        android:layout_marginLeft="20dp"
        android:layout_marginTop="10dp"
        android:background="#00FFFFFF"
        android:scaleType="centerInside"
        app:layout_constraintStart_toEndOf="@+id/prevButton_mediaBox"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@drawable/greenbox"
        tools:visibility="visible" />

    <ImageButton
        android:id="@+id/stopButton_mediaBox"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:layout_marginTop="10dp"
        android:layout_marginEnd="20dp"
        android:layout_marginRight="20dp"
        android:background="#00FFFFFF"
        android:scaleType="centerInside"
        app:layout_constraintEnd_toStartOf="@+id/nextButton_mediaBox"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@drawable/bluebox"
        tools:visibility="visible" />

    <ImageButton
        android:id="@+id/nextButton_mediaBox"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:layout_marginTop="10dp"
        android:layout_marginEnd="16dp"
        android:layout_marginRight="16dp"
        android:background="#00FFFFFF"
        android:scaleType="centerInside"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@drawable/redbox"
        tools:visibility="visible" />
</androidx.constraintlayout.widget.ConstraintLayout>